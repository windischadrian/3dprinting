const printers = {
    voron: 'http://mainsail.local',
    ender: 'http://ender.local'
}

module.exports = printers

// Object.keys(printers).forEach(printer => {
//     console.log(printer + "-" + printers[printer])
// })